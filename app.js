const swatches=[...document.querySelectorAll('.swatch')];const lookName=document.querySelector('#lookName');swatches.forEach(s=>s.addEventListener('click',()=>{swatches.forEach(x=>x.classList.remove('active'));s.classList.add('active');lookName.textContent=s.dataset.name;document.querySelector('.avatar-stage').style.background=`linear-gradient(145deg, ${s.style.getPropertyValue('--swatch')}88, #f4d2cf)`}));document.querySelector('#shuffle').addEventListener('click',()=>{const next=swatches[(swatches.findIndex(x=>x.classList.contains('active'))+1)%swatches.length];next.click()});document.querySelector('.menu').addEventListener('click',()=>document.querySelector('.nav-links').classList.toggle('mobile-open'));
